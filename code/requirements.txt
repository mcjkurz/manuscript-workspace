# Python dependencies for the code/ environment.
# Install with:
#   venv/bin/pip install -r requirements.txt
#
# System requirements (not pip-installable; see README.md):
#   - quarto      (Markdown -> DOCX)
#   - xelatex / latexmk / biber  (LaTeX -> PDF, default engine: xelatex)

# --- core numerics / plotting ---
numpy
matplotlib
scikit-learn
joblib

# --- streaming / IO ---
ijson
tqdm

# --- XML / MediaWiki / DOCX ---
lxml
mwparserfromhell
python-docx

# --- Chinese text processing ---
jieba
opencc-python-reimplemented
qhchina
